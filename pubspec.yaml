name: integrated
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  badges: ^2.0.1 # Correctly add this under dependencies
  google_maps_flutter: ^2.10.0  # From maps project
  geolocator: ^13.0.2  # From maps project
  geocoding: ^3.0.0  # From maps project
  cupertino_icons: ^1.0.8
  http: ^0.13.0  # From maps project
  flutter_typeahead: ^5.2.0  # From maps project
  google_place: ^0.4.7  # From maps project
  build_runner: ^2.4.14  # From maps project

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/map_style.json
    - assets/places_data.json  # Assets for maps project

